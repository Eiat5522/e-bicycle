{
  "name": "@glide/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --emitDeclarationOnly --declaration --declarationMap --outDir dist",
    "lint": "eslint src",
    "test": "jest --config ./jest.config.cjs --runInBand",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@glide/shared": "workspace:*"
  }
}
